import Image from "next/image";
import LogoFull from "../../assets/logo-full.png";

const MenuItem: React.FC<{
  url: string;
  children: string;
}> = ({ url, children }) => {
  return (
    <li>

    </li>
  );
};

export const Header = () => {
  return (
    <div>

    </div>
  );
};
